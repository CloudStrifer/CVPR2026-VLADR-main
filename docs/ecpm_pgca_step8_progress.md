# ECPM / PGCA 第 8 步执行记录

日期：2026-09-14。已完成：连续阶段流程与完整优化器更新边界的精确恢复。

- [x] A. 检查既有步骤，确定恢复语义和保存内容。
- [x] B. 提取共用优化器更新，新增可恢复 Trainer、确定性采样游标和状态校验。
- [x] C. 保存/恢复教师、分类头、优化器、scaler、随机数、旧 ECPM 与未提交候选；不重复迁移。
- [x] D. 连续阶段入口、不可变参考文件、原子 latest 断点、日志回滚及安全暂停。
- [x] E. 中断对照测试、前 7 步回归、真实 CLIP 恢复验证。
- [x] F. 使用说明、总计划及恢复记录更新。

## 已确定工程边界

用户本次明确要求精确恢复，因此超过原计划“仅阶段边界恢复”的最低建议，实现阶段内完整优化器更新边界恢复。不保存一次反向传播中途的计算图；异常时回到最后成功落盘的更新，重做尚未保存的更新。

首版精确路径限定 workers=0、单进程单设备和内置数据增强，记录并校验运行环境/配置。每类别从确定性 P×K 采样索引按保存游标获取当前图片，不为跳过旧批次重新读取图片或消耗增强随机数。

不可变 reference.pt 保存冻结主干与架构，latest.pt 绑定其文件哈希；频繁断点保存学生 Adapter、当前分类头、AdamW、AMP scaler、原教师 bank、固定权重、迁移元数据、ECPM 旧状态/当前候选、进度和全局 RNG。不重新初始化迁移/教师，也不提前提交原型。

日志需与断点中的字节位置一致；崩溃后多出的尾部归档，再从已验证前缀接续，避免重复计数。第 9 步正式路由评估暂不实现。

开发内容分段落盘，恢复时从首个未完成项继续。

## 开发检查点（2026-09-13）

已新增连续入口、可恢复 Trainer、原子文件/RNG/日志事务。首轮 11 项恢复测试中，逐步参数、优化器、教师、RNG 和三阶段日志对照均通过；最后一个身份数量断言用了错误字段名，已修正。正在补充梯度裁剪顺序稳定性、阶段耗时/存储日志，随后重跑回归并验证真实 CUDA AMP。当前尚不能宣布验收完成。

## 最终验收（2026-09-14）

额度中断后核对磁盘发现，上轮后台回归和真实 CLIP 验证均已成功完成。本次从这些结果接续，没有重做已通过的长验证。

- `docs/step8_regression_tests.txt`：173 项检查通过。此前使用 stdin 启动测试导致 Windows 多进程 worker 无法导入主模块；已改为正常 `python -m unittest discover` 命令，worker 回归通过。
- `docs/step8_resume_tests.txt`：最终 13 项恢复专项检查通过（15.681 秒）。覆盖阶段内优化器/教师/分类头/RNG 对照、提交前最后更新、准备中断、日志尾部回滚、日志前缀篡改、配置/参考文件变化、损坏游标/教师、禁止旧/未来图片读取、CLI 恢复、原子替换失败、RNG 高斯缓存；补充提交落盘失败及不等长采样轮次检查。
- `docs/progressive_resume_smoke.json`：真实 CLIP ViT-B/16、CUDA＋AMP、默认随机增强、梯度裁剪、三阶段 18 次更新，通过独立 Python 进程恢复。第 8 次更新后恢复到第 9 次更新，与连续到第 9 次的基准状态逐位一致；随后双方继续至第 3 阶段，Adapter、原型、RNG 和训练日志均一致。CPU 测试额外覆盖保持基准对象连续运行至最终阶段的对照。
- 真实短训练参考文件 344,818,959 字节；T2 阶段内断点 14,446,411 字节。样例类别/身份少，不能据此推断正式实验固定内存成本。

核心文件：

1. `train_category_progressive.py`：连续阶段入口、阶段状态转换、配置与历史校验、暂停、提交和日志。
2. `reid/trainer_category_resumable.py`：完整更新、采样游标、部分 epoch 统计、学生/教师/头/优化器/scaler 状态恢复。
3. `reid/utils/progressive_checkpoint.py`：原子保存、RNG、确定性环境、参考/代码哈希、日志前缀事务。
4. `reid/trainer_category_progressive.py`：提取共用损失与更新逻辑，并固定梯度裁剪的参数顺序。
5. `tests/test_progressive_resume.py`、`tools/smoke_test_progressive_resume.py`：恢复验收。
6. `docs/progressive_training_usage_zh.md`：完整使用说明。

## 下次继续的位置

本步没有未完成实现。下一次用户要求继续时，从总计划第 9 步“硬路由、终身评估和必要消融”开始。先读第 8 步使用说明、连续训练入口及总计划第 9 步，再接入评估流程。当前尚无正式无类别标签硬路由评估，也没有真实数据精度结论。

若只是恢复某次训练：保留完整输出目录，以 `python train_category_progressive.py --output-dir <该目录> --resume` 继续。该入口保存软件、设备、线程、数据流和代码指纹；修改训练代码后不能绕过校验声称对旧断点精确恢复。不要对未完成阶段重新执行迁移、改用当前学生作为旧教师，或提前提交候选原型。
